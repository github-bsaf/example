on: push

env:
  JENKINS_HOST: https://jenkins-gldtn-tv2-sdk.cmo.conti.de
  JENKINS_JOB: job/pipeline_fbl
  JENKINS_USER: uig45131
  JENKINS_TOKEN: 11d159eda41f6ec09a9b29a66aa44b9d7e

jobs:
  launch_jenkins_job:
    name: trigger jenkins job
    runs-on: ubuntu-latest
    steps:
      -
        name: Trigger Jenkins Job
        uses: sbitio/trigger-jenkins-job@master
        with:
          JENKINS_USER: ${{ env.JENKINS_USER }}
          JENKINS_TOKEN: ${{ env.JENKINS_TOKEN }}
          JENKINS_HOST: ${{ env.JENKINS_HOST }}
          JENKINS_JOB: ${{ env.JENKINS_JOB }}
